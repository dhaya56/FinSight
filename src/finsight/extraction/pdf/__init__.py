"""The native PDF extraction path.

``pymupdf_adapter`` is the only module in FinSight that imports a PDF library,
so a second producer is a sibling file rather than a change spread across the
codebase.
"""
